standard
	每次都加载一个新的activity 加入栈
singleTop
	如果栈顶已经是 要加载的a了 那么就不会进行加载 所以 栈顶的两个元素不可能重复(如果它是singleTop的)
singleTask
	同一个任务栈里只能有一个task(所以不同的任务栈里就可以有多个)
	假设要切换到Activity a
	假设a不存在 那么就创建一个 加入栈
	如果a已经存在了,那么就弹出a上面的所有任务直到a
singleInstance
	整个系统里只能有一个a a有自己的一个独有的任务栈 每次切换到a的时候 都会进行任务站的切换以保持单例
